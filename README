1. Using the XMLRPC-c in C to create mutual remote procedure call between two hosts 
2. Simple data struct transfering protocol with string and int.
3. Run abyss server on another interruptable thread to execute requests from other servers